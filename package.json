{
  "name": "chatgpt-export",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted, resumable, read-only backup of web-accessible ChatGPT account data.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/exporter.mjs && node --check src/chatgpt.mjs && node --check src/storage.mjs && node --check src/crypto.mjs"
  },
  "license": "MIT"
}
